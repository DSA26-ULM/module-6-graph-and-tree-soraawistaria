#include <bits/stdc++.h>
using namespace std;

int main() {
    int R, C, x_awal, y_awal, x_tuju, y_tuju;
    cin >> R >> C;

    int G[R][C];
    for (int i=0; i<R; i++) {
        for (int j=0; j<C; j++) {
            cin >> G[i][j];
        }
    }
    cin >> x_awal >> y_awal;
    cin >> x_tuju >> y_tuju;
    
}